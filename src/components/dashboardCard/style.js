import { makeStyles } from '@material-ui/core/styles';

const styles = {
  paper: {
    boxShadow: '0 4px 8px 0 rgba(0,0,0,.32) !important',
    padding: '16px',
  },
};

export default makeStyles(styles);
